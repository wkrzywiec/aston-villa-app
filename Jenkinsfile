pipeline {

    agent any
    stages {
        stage('Verify') {                 
            steps {
                echo "$GIT_BRANCH"             
            }
        }

        stage('Build') {                 
            steps {
                echo "$GIT_BRANCH"             
            }
        } 

        stage('Deploy') {                 
            steps {
                echo "$GIT_BRANCH"             
            }
        } 
    }
}
