pipeline {

    agent any
    stages {
        stage('Verify') {                 
            steps {
                echo "$GIT_BRANCH"             
            }
        }

        stage('Verify') {                 
            steps {
                echo "$GIT_BRANCH"             
            }
        } 

        stage('Verify') {                 
            steps {
                echo "$GIT_BRANCH"             
            }
        } 
    }
}
